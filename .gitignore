# ── Backend (Python / FastAPI) ────────────────────────────────────────────
backend/venv/
backend/.venv/
**/__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Real secrets — never commit these (see backend/.env.example for the template)
backend/.env
.env

# ── Android / Gradle ───────────────────────────────────────────────────────
android/.gradle/
android/**/build/
android/local.properties
android/.idea/
android/**/*.iml
android/.cxx/
android/captures/
android/*.hprof

# ── Claude Code / Cowork local tooling (not part of the app) ──────────────
# Plugin cache, skills, and local settings — machine-specific, not project source.
.claude/
.idea/

# ── OS / editor junk ────────────────────────────────────�